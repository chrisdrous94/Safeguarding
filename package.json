{
  "name": "safeguarding-dev-checks",
  "private": true,
  "description": "Dev-only validation harness (syntax check + jsdom smoke test). Not a runtime dependency of the app — the app itself is a static index.html talking to Apps Script.",
  "scripts": {
    "check": "node scripts/check-syntax.js",
    "smoke": "node scripts/smoke-test.js",
    "test": "npm run check && npm run smoke"
  },
  "devDependencies": {
    "jsdom": "^25.0.1"
  }
}
